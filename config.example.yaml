# Copy to config.yaml. Run: tfcvars -f config.yaml   (or: go run . -f config.yaml)
# Do not commit files that contain a real token.
# If token is omitted, TFC_TOKEN / TF_TOKEN is used instead.
#
# hostname: https://app.terraform.io   # optional; default from TFC_ADDRESS or TFC public cloud
token: your-terraform-cloud-api-token

org: your-org

workspaces:
  - name: aws-dev
    variables:
      - key: secret_key1
        value: secret123
      - key: secret_key2
        value: secret123
        # category: terraform   # default terraform; use env for environment variables
        # sensitive: true       # default true
        # hcl: false            # default false
        # description: Slack webhook for distribution alerts

  # - name: aws-uat
  #   org: other-org          # optional; defaults to top-level org
  #   variables:
  #     - key: slack_api_url_distribution
  #       value: https://hooks.slack.com/...
